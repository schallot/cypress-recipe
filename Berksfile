site :opscode

metadata

# Since berks will pull all the data it needs from the metadata.rb, the only thing needed here is 
# a reference to the custom rvm recipe which we are using which is not in opscode.

cookbook 'rvm', github: 'fnichol/chef-rvm', ref: '0cd320911c1e568cbf2eeac895a5ef06e5821589'